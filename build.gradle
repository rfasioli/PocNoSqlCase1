plugins {
    id 'java'
}

allprojects {
    group 'org.example'
    version '1.0-SNAPSHOT'

    sourceCompatibility = 1.8

    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    dependencies {
    }
}
